name: CI Pipeline

on: [push]
jobs:
   build:
    runs-on: ubuntu-latest
    steps:
      -name: Checkoutcode
       uses: action/checkout@v3
      -name: Run script
       run: echo "Hello Devops CI/CD pipeline"
